# Configuration for @aderant scoped packages
# These packages are hosted in GitHub Packages and require authentication

# Configure GitHub Packages registry for @aderant scope
@aderant:registry=https://npm.pkg.github.com

# Authentication token (uses environment variable for security)
# Set GITHUB_TOKEN environment variable before running pnpm install
# For local development: export GITHUB_TOKEN=ghp_your_token_here
# For Docker builds: Passed as build arg (see Dockerfile)
# For GitHub Actions: Automatically available as secrets.GITHUB_TOKEN
//npm.pkg.github.com/:_authToken=${GHCR_READ_TOKEN}

# pnpm configuration
auto-install-peers=true
strict-peer-dependencies=false
