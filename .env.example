# Environment file for development
# Copy this to .env.development (or .env.local) if you need to customize

# API Configuration (Optional)
# If you have a backend API, set this to your API base URL
# If not set, the app will use a mock user for development
# UI_API_BASE_URL=http://localhost:8000

# Mock User Customization (Optional)
# Customize the mock user without editing code
# UI_MOCK_USER_FIRST_NAME=Hackathon
# UI_MOCK_USER_LAST_NAME=Developer
# UI_MOCK_USER_EMAIL=developer@hackathon.local

# MUI X License Key (Optional)
# Only needed if using MUI X Pro components
# UI_MUI_X_LICENSE_KEY=